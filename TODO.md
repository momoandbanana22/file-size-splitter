# TODO

## 管理ルール

- **優先度セクション**: まだ完了していないタスク（チェックマークなし）
- **完了済みセクション**: 完了したタスク（チェックマークなし、単なるリスト）
- タスクが完了したら、優先度セクションから削除し、完了済みセクションに移動

## 優先度: 高

なし

## 優先度: 中

- ビルドしたパッケージの動作確認を自動化するテストを追加
  - 背景: プロジェクトルールの検証プロセスに「パッケージ動作確認」が含まれているが、自動化されていなかった
    - 既存の自動テスト（test_ps1_script_execution）は開発中のパッケージ（editable install）に対してテストしている
    - ビルドしたパッケージ（dist/以下のwhlファイル）をインストールして動作確認するプロセスが自動化されていなかった
    - 手動で確認を行ったが、検証プロセスを完全に自動化する必要がある
  - テストファイル名: tests/test_built_package.py
  - 実装方法: subprocessでビルド済みパッケージをpip install → テスト実行 → pip uninstall
  - 対象テスト: バイナリファイル（ZIP）での分割・復元テスト、PS1スクリプト実行テスト
  - 実行タイミング: pytest -m package で実行（通常のpytestからは除外）
  - 環境: Windows環境でのみ実行
  - 目的: 開発中のパッケージではなく、ビルドしたパッケージに対してテストを行う

## 優先度: 低

なし

## 完了済み

- PowerShellスクリプトによるバイナリファイル復元バグの修正
  - PowerShellのGet-Content -Rawがバイナリファイルを正しく扱えない問題を修正
  - .NETの[System.IO.File]::ReadAllBytes()/WriteAllBytes()を使用してバイナリモードで結合するように実装
  - Python側の分割処理はバイナリモードで正しく実装されていることを確認
- テストカバレッジの改善
  - ZIPファイルなどのバイナリファイルでの分割・復元テストを追加（Windows環境でのみ実行）
  - 生成されたPS1スクリプトを実際に実行して復元を検証するテストを追加（Windows環境でのみ実行、subprocessでPowerShellを起動）
  - 分割ファイル自体のバイナリデータが正しいかの直接検証テストを追加（Windows環境でのみ実行）
- プロジェクト構造の作成
- ファイル分割機能の実装
- BATファイル生成機能の実装
- PS1ファイル生成機能の実装
- CLIの実装
- テストの作成
- PyPI公開
- バージョン管理の改善
- ドキュメントの改善（バッジ追加、ライセンス表現修正）
- LICENSEファイルの作成
- ライブラリとしての使用方法の改善（__init__.pyで関数をexport）
- sha512ハッシュ検証機能の追加
  - ファイル分割時
    - 分割前のオリジナルのsha512ハッシュと、分割した各ファイルのsha512ハッシュを計算してmetadata.jsonに保存
    - 分割ファイルの作成直後にその場で復元確認を行う（sha512ハッシュ検証と、実際に復元を行い、元のファイルと比較）
  - ファイル復元時
    - BAT/PS1スクリプトでsha512ハッシュを検証してファイルの整合性を確認する機能を追加（復元前に各ファイルのsha512ハッシュを検証、復元後に元のファイルのsha512ハッシュと比較）
    - BAT/PS1スクリプトでハッシュ検証を実装（PowerShellのGet-FileHashコマンドを使用）
  - metadata.jsonにsha512ハッシュ情報を追加（original_sha512、各partのsha512）
- requirements.txtの作成
- README.mdの更新（インストール方法とライブラリ使用方法の整合性改善）
- BATファイルの簡素化（PS1スクリプトを起動するだけに変更）
